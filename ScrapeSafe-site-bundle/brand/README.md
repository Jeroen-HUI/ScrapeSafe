Brand assets for the marketing site.

- **`logo.png`** — App mark (teal S + map pin). Header on `index.html` links home with this mark + wordmark.
- Optional: add `wordmark.svg` later if you replace the text lockup.
